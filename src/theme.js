import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
    colors: {
        main:{
            // Define main theme color (100-900)
            900: "#000000"
        }
    }
})

export default theme